import { makeStyles } from "@material-ui/core";

export const useStyle = makeStyles((theme) => ({
  search: {
    width: "60%",
  },
  venueName: {
    fontSize: "16px",
    margin: "0 2px",
    fontWeight: "2px",
  },
  venueSubtitle: {
    fontSize: "12px",
    margin: "0 2px",
  },
}));
